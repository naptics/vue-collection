{
    "extends": "./tsconfig.app.json",
    "include": ["src/lib/**/*", "src/lib/**/*.json", "src/lib/**/*.css"],
    "exclude": ["src/**/__tests__/*"],
    "compilerOptions": {
        "composite": true,
        // don't allow @ alias inside library
        "paths": {},
        "outDir": "lib"
    }
}
